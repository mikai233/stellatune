// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.13.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../../frb_generated.dart';

import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

// These types are ignored because they are neither used by any `pub` functions nor (for structs and enums) marked `#[frb(unignore)]`: `ProbeResult`, `ProbeStatus`
// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `eq`, `eq`, `eq`, `eq`, `eq`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`

class AudioProperties {
  final String? format;
  final String? codec;
  final int? sampleRate;
  final int? bitsPerSample;
  final bool floatingPoint;
  final int? channels;
  final BitrateInfo? bitrate;

  const AudioProperties({
    this.format,
    this.codec,
    this.sampleRate,
    this.bitsPerSample,
    required this.floatingPoint,
    this.channels,
    this.bitrate,
  });

  static Future<AudioProperties> default_() => StellatuneApi.instance.api
      .stellatuneMediaProbeTypesAudioPropertiesDefault();

  @override
  int get hashCode =>
      format.hashCode ^
      codec.hashCode ^
      sampleRate.hashCode ^
      bitsPerSample.hashCode ^
      floatingPoint.hashCode ^
      channels.hashCode ^
      bitrate.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is AudioProperties &&
          runtimeType == other.runtimeType &&
          format == other.format &&
          codec == other.codec &&
          sampleRate == other.sampleRate &&
          bitsPerSample == other.bitsPerSample &&
          floatingPoint == other.floatingPoint &&
          channels == other.channels &&
          bitrate == other.bitrate;
}

class BitrateInfo {
  final int bps;
  final BitrateKind kind;
  final bool estimated;
  final BitrateMode? mode;

  const BitrateInfo({
    required this.bps,
    required this.kind,
    required this.estimated,
    this.mode,
  });

  @override
  int get hashCode =>
      bps.hashCode ^ kind.hashCode ^ estimated.hashCode ^ mode.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BitrateInfo &&
          runtimeType == other.runtimeType &&
          bps == other.bps &&
          kind == other.kind &&
          estimated == other.estimated &&
          mode == other.mode;
}

enum BitrateKind { fixed, average, nominal }

enum BitrateMode { cbr, vbr, abr }
