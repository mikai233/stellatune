// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.13.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../../frb_generated.dart';

import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

// These types are ignored because they are neither used by any `pub` functions nor (for structs and enums) marked `#[frb(unignore)]`: `PluginLibraries`, `PluginLibraryInstance`
// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `eq`, `eq`, `eq`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `hash`, `hash`

// Rust type: RustOpaqueMoi<flutter_rust_bridge::for_generated::RustAutoOpaqueInner<LocalCatalog>>
abstract class LocalCatalog implements RustOpaqueInterface {
  Future<CatalogPage> browse({required CatalogQuery query});

  Future<CatalogItem> detail({required MediaRef reference});
}

class CatalogItem {
  final MediaRef reference;
  final String title;
  final String? artist;
  final String? album;
  final PlatformInt64? durationMs;
  final PlatformInt64? trackCount;
  final String? artworkUrl;
  final PlatformInt64? localTrackId;
  final String? localPath;
  final MediaRef? albumRef;
  final List<MediaRef> artistRefs;

  const CatalogItem({
    required this.reference,
    required this.title,
    this.artist,
    this.album,
    this.durationMs,
    this.trackCount,
    this.artworkUrl,
    this.localTrackId,
    this.localPath,
    this.albumRef,
    required this.artistRefs,
  });

  @override
  int get hashCode =>
      reference.hashCode ^
      title.hashCode ^
      artist.hashCode ^
      album.hashCode ^
      durationMs.hashCode ^
      trackCount.hashCode ^
      artworkUrl.hashCode ^
      localTrackId.hashCode ^
      localPath.hashCode ^
      albumRef.hashCode ^
      artistRefs.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is CatalogItem &&
          runtimeType == other.runtimeType &&
          reference == other.reference &&
          title == other.title &&
          artist == other.artist &&
          album == other.album &&
          durationMs == other.durationMs &&
          trackCount == other.trackCount &&
          artworkUrl == other.artworkUrl &&
          localTrackId == other.localTrackId &&
          localPath == other.localPath &&
          albumRef == other.albumRef &&
          artistRefs == other.artistRefs;
}

class CatalogPage {
  final List<CatalogItem> items;
  final String? nextCursor;
  final PlatformInt64? total;

  const CatalogPage({required this.items, this.nextCursor, this.total});

  @override
  int get hashCode => items.hashCode ^ nextCursor.hashCode ^ total.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is CatalogPage &&
          runtimeType == other.runtimeType &&
          items == other.items &&
          nextCursor == other.nextCursor &&
          total == other.total;
}

class CatalogQuery {
  final String sourceInstanceId;
  final MediaKind kind;
  final MediaRef? parent;
  final String search;
  final CatalogSort sort;
  final String? cursor;
  final int limit;

  const CatalogQuery({
    required this.sourceInstanceId,
    required this.kind,
    this.parent,
    required this.search,
    required this.sort,
    this.cursor,
    required this.limit,
  });

  Future<void> validate() => StellatuneApi.instance.api
      .stellatuneLibraryCatalogCatalogQueryValidate(that: this);

  @override
  int get hashCode =>
      sourceInstanceId.hashCode ^
      kind.hashCode ^
      parent.hashCode ^
      search.hashCode ^
      sort.hashCode ^
      cursor.hashCode ^
      limit.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is CatalogQuery &&
          runtimeType == other.runtimeType &&
          sourceInstanceId == other.sourceInstanceId &&
          kind == other.kind &&
          parent == other.parent &&
          search == other.search &&
          sort == other.sort &&
          cursor == other.cursor &&
          limit == other.limit;
}

enum CatalogSort { default_, title }

class LibrarySource {
  final String id;
  final String name;
  final bool local;
  final bool available;
  final String? error;
  final List<MediaKind> browseKinds;
  final List<MediaKind> searchKinds;
  final List<CatalogSort> sorts;

  const LibrarySource({
    required this.id,
    required this.name,
    required this.local,
    required this.available,
    this.error,
    required this.browseKinds,
    required this.searchKinds,
    required this.sorts,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      name.hashCode ^
      local.hashCode ^
      available.hashCode ^
      error.hashCode ^
      browseKinds.hashCode ^
      searchKinds.hashCode ^
      sorts.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is LibrarySource &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          name == other.name &&
          local == other.local &&
          available == other.available &&
          error == other.error &&
          browseKinds == other.browseKinds &&
          searchKinds == other.searchKinds &&
          sorts == other.sorts;
}

enum MediaKind { track, album, artist, folder, playlist }

class MediaRef {
  /// Decimal host identity; plugin IDs never substitute for this namespace.
  final String sourceInstanceId;
  final MediaKind kind;
  final String id;

  const MediaRef({
    required this.sourceInstanceId,
    required this.kind,
    required this.id,
  });

  @override
  int get hashCode => sourceInstanceId.hashCode ^ kind.hashCode ^ id.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is MediaRef &&
          runtimeType == other.runtimeType &&
          sourceInstanceId == other.sourceInstanceId &&
          kind == other.kind &&
          id == other.id;
}
